# Example `.synapse` Layout

Synapse stores local runtime state in `.synapse/`. Real runtime state is ignored by Git; this example directory documents the intended layout.

```text
.synapse/
  synapse.db
  objects/
  snapshots/
  graph/
  vector/
  logs/
```

